{
  "name": "robogo-learning-portal",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": ".venv311/bin/python -m uvicorn backend.app.main:app --reload --host 127.0.0.1 --port 3001",
    "start:node-prototype": "node server.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
